package jext.buildtools.resource;

import jext.buildtools.Module;

import java.io.File;

public class XMLResourceFile extends ResourceFile {

    public XMLResourceFile(File resourceFile, Module module) {
        super(resourceFile, module);
    }
}
